apiVersion: apps/v1
kind: Deployment
metadata:
  name: distributed-semaphore
spec:
  replicas: 3
  template:
    spec:
      # Pin this deployment's pods to nodes in data center 1.
      nodeSelector:
        topology.kubernetes.io/region: dc1
      containers:
        - name: app
          env:
            - name: DATA_CENTER
              value: "dc1"
