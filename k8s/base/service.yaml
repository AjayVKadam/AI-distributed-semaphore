apiVersion: v1
kind: Service
metadata:
  name: distributed-semaphore
  labels:
    app.kubernetes.io/name: distributed-semaphore
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: distributed-semaphore
  ports:
    - name: http
      port: 80
      targetPort: http
