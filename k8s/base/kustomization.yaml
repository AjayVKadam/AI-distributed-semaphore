apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# secret.example.yaml is intentionally NOT included here; provide the real
# mongodb-credentials Secret per cluster out-of-band.
resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/part-of: distributed-semaphore
